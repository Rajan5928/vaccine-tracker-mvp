const VaccinationSubmission = {
    patient: String, // Store patient ID as a string
    vaccineName: String,
    submissionDate: Date,
    isVerified: Boolean,
};

module.exports = VaccinationSubmission;